# 0x02. Shell, I/O Redirections and Filters
This project explores how to manipulate data streams using:
* Standard Input, Output, and Error redirection (>, >>, |)
* Text processing filters (grep, sort, uniq, tr, tail, head)
* Finding and managing files via the command line
